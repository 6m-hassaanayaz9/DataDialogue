-- database: d:\freelancing_work_data\upwork_job_material\jobinprc1\Todo_List\fyp_backend\datadialgue_bckend\db.sqlite3

-- Use the ▷ button in the top right corner to run the entire file.

SELECT * FROM user;

ALTER TABLE user
ADD COLUMN access_token VARCHAR(255);

ALTER TABLE user
ADD COLUMN refresh_token VARCHAR(255);
